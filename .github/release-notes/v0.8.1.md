## 官方 `.627` 基线与发布流程修正

相对 v0.8.0：

- 将安装器兼容基线锚定到 Steam 分发的官方 `0.0.9.627/feapp.dat`。此前版本使用维护者本机的重打包产物，Steam 全新安装用户会在 `baseline:import` 时失败。
- 正式发布改为由 GitHub Actions 在 Windows Runner 上构建，并执行 JavaScript 语法、运行时白名单和安装包 SHA-256 等检查；Gitee 使用同一批构建产物同步。
- 安装前置说明明确要求备份游戏目录；安装器仍会创建可恢复的客户端备份。
