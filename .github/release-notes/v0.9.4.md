## 安装载荷解压兼容性

相对 v0.9.3：

- 修复部分机器使用资源管理器或其他解压工具处理旧安装载荷后，把 `tools/backup.mjs` 等目标文件名错误建立为目录，导致安装失败并显示误导性的 `PathNotFound` 错误。
- 安装器解压前会清理与载荷文件同名的目录，并优先使用 Windows 自带的 `tar.exe`（bsdtar）；不适用时仍回退到 `Expand-Archive`。
