# v0.9.6

## 修复

- 修复“测试当前模型”在存在供应商和模型时仍提示 `Provided value cannot be bound to SQLite parameter 1` 的问题。
- 模型测试现在使用明确的临时信件 ID，仅用于排除历史，不会写入邮箱、数据库或生成队列。

## 保持不变

- 本版本继续使用本地 SQLite、模型设置界面和现有多供应商编排。
- 安装器继续内置 Node.js，并保留 Windows PowerShell 5.1/7 解压冒烟检查。
- 项目不包含游戏本体、官方资源或 API Key；安装前仍应备份游戏目录。
