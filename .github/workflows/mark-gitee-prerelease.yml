name: Mark legacy Gitee releases as pre-release
on:
  workflow_dispatch:
jobs:
  mark:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node .github/scripts-once/gitee-prerelease.mjs
        env:
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
